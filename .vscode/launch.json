{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "cpuonly: Debug with gdb server",
            "type": "cortex-debug",
            "request": "launch",

            "servertype": "external",
            "gdbTarget": "localhost:2331",

            "device": "Cortex-M7",
            "interface": "swd",

            "cwd": "${workspaceFolder}/cpuonly",
            "executable": "${workspaceFolder}/cpuonly/cpuonly.elf",

            "gdbPath": "/usr/bin/gdb-multiarch",
            "objdumpPath": "/usr/bin/llvm-objdump",

            "preLaunchTask": "cpuonly: build",

            "overrideLaunchCommands": [
                "monitor reset",
                "-target-download",
                "set $pc = soft_entry"
            ],

            "overrideResetCommands": [
                "monitor reset",
                "set $pc = soft_entry"
            ],

            "runToEntryPoint": "_start",
            "showDevDebugOutput": "raw"
        }
    ]
}
